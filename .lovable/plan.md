

# Remove Double Max-Width Constraint

Two changes:

**1. `src/pages/Index.tsx` line 80**
Change `max-w-7xl mx-auto` to `w-full px-4`

**2. `src/components/views/SessionView.tsx` line 273**
Change `max-w-3xl mx-auto` to `w-full` on the main session container

No other changes. Mobile layout unchanged.

